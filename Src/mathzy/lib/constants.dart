// --- DURATION CONSTANTS ---
const Duration kModelReadyDisplayDuration = Duration(milliseconds: 1000);
const Duration kModelDownloadedDisplayDuration = Duration(seconds: 1);
const Duration kFeedbackOverlayDisplayDuration = Duration(milliseconds: 500);
const Duration kMainFeedbackClearDelay = Duration(milliseconds: 1500);
const Duration kAutoSubmitScribbleDelay = Duration(milliseconds: 700);
const Duration kConfettiDuration = Duration(seconds: 1);
const Duration kListenForDuration = Duration(minutes: 1);
const Duration kPauseForDuration = Duration(seconds: 2);
const Duration kListeningBlobAnimationDuration = Duration(milliseconds: 700);
const Duration kPulsingDotAnimationDuration = Duration(
  milliseconds: 700,
); // Defined
// --- END DURATION CONSTANTS ---
